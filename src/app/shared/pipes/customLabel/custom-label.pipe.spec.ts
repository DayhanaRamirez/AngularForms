import { CustomLabelPipe } from './custom-label.pipe';

describe('CustomLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
